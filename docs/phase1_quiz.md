# Rust フェーズ1 確認テスト

このテストは、Rust学習フェーズ1で学んだ内容の理解度を確認するためのものです。
全問題を通じて、Rustの基本的な文法と概念を理解できているかを確認します。

---

## 第1部：理解度確認問題（各5点、計40点）

### 問題1：Rust開発環境について
以下の質問に答えてください：

1. `rustup` とは何ですか？その役割を簡潔に説明してください。
2. `cargo` の主な役割を3つ挙げてください。
3. `cargo new` コマンドで作成されるプロジェクトの基本的なディレクトリ構造を説明してください。

### 問題2：基本的なデータ型
以下のコードの中で、コンパイルエラーになる行はどれですか？理由も説明してください。

```rust
fn main() {
    let x = 5;              // 1
    let y: i32 = 5.0;       // 2
    let z: u8 = -1;         // 3
    let w: char = "A";      // 4
    let v: bool = true;     // 5
    let u = 3.14;           // 6
}
```

### 問題3：可変性と不変性
以下のコードを修正して、コンパイルが通るようにしてください。また、なぜ元のコードがエラーになるか説明してください。

```rust
fn main() {
    let x = 5;
    println!("xの値は: {}", x);
    x = 10;
    println!("xの新しい値は: {}", x);
}
```

### 問題4：関数について
以下の質問に答えてください：

1. Rustの関数で戻り値を返す方法を2つ挙げてください。
2. 関数の引数と戻り値には型注釈が必要ですか？その理由も説明してください。

### 問題5：所有権の基礎
以下のコードがなぜコンパイルエラーになるか説明してください（まだ詳しく学んでいませんが、フェーズ1でも触れた内容です）。

```rust
fn main() {
    let s = String::from("hello");
    let s2 = s;
    println!("{}", s);
}
```

### 問題6：制御フロー
`if` 式の特徴について、以下の質問に答えてください：

1. Rustの `if` が「式」であることの意味は何ですか？
2. `if` 式を使って変数に値を代入する例を示してください。

### 問題7：ループ
`loop`、`while`、`for` の使い分けについて、それぞれどのような場面で使うのが適切か説明してください。

### 問題8：マクロ
`println!` は関数ではなくマクロです。マクロ呼び出しの特徴的な記号は何ですか？また、なぜ `println` が関数ではなくマクロとして実装されているか推測してください。

---

## 第2部：コーディング問題（各10点、計60点）

### 問題9：基本的な変数操作（10点）
以下の要件を満たすプログラムを作成してください：

1. 整数型、浮動小数点型、文字型、ブーリアン型の変数をそれぞれ1つずつ宣言する
2. それらの値を使って簡単な計算や操作を行う
3. 結果を見やすく表示する

```rust
fn main() {
    // ここにコードを書いてください
}
```

### 問題10：関数の実装（10点）
摂氏を華氏に変換する関数 `celsius_to_fahrenheit` を実装してください。
- 引数：摂氏温度（f64型）
- 戻り値：華氏温度（f64型）
- 変換式：華氏 = 摂氏 × 9/5 + 32

```rust
fn main() {
    let celsius = 25.0;
    let fahrenheit = celsius_to_fahrenheit(celsius);
    println!("{}°C は {}°F です", celsius, fahrenheit);
}

// ここに関数を実装してください
```

### 問題11：条件分岐の活用（10点）
年齢を受け取って、以下の分類を行う関数 `age_category` を実装してください：
- 0-12歳: "子供"
- 13-19歳: "ティーンエイジャー"
- 20-64歳: "大人"
- 65歳以上: "シニア"
- 負の値: "無効な年齢"

```rust
fn main() {
    let ages = vec![5, 15, 30, 70, -5];
    for age in ages {
        println!("{}歳は{}です", age, age_category(age));
    }
}

// ここに関数を実装してください
```

### 問題12：ループの活用（10点）
1から指定された数までのフィボナッチ数列を計算して表示するプログラムを作成してください。
- `for` ループまたは `while` ループを使用すること
- 最初の2つの数は0と1とする

```rust
fn main() {
    let n = 10; // 最初の10個のフィボナッチ数を表示
    println!("最初の{}個のフィボナッチ数:", n);

    // ここにコードを書いてください
}
```

### 問題13：総合問題 - 簡単な計算機（10点）
以下の要件を満たす簡単な計算機プログラムを作成してください：

1. 2つの数値と演算子（+, -, *, /）を受け取る関数 `calculate` を実装
2. 0での除算の場合は適切なメッセージを返す
3. 不正な演算子の場合もエラーメッセージを返す

```rust
fn main() {
    // テストケース
    println!("{}", calculate(10.0, 5.0, '+'));
    println!("{}", calculate(10.0, 5.0, '-'));
    println!("{}", calculate(10.0, 5.0, '*'));
    println!("{}", calculate(10.0, 5.0, '/'));
    println!("{}", calculate(10.0, 0.0, '/'));
    println!("{}", calculate(10.0, 5.0, '%'));
}

// ここに関数を実装してください
fn calculate(a: f64, b: f64, op: char) -> String {
    // 実装してください
}
```

### 問題14：統合演習 - 成績評価システム（10点）
学生の点数（0-100）を受け取って、成績（A, B, C, D, F）と合否を判定するプログラムを作成してください。

要件：
- 90点以上: A（合格）
- 80-89点: B（合格）
- 70-79点: C（合格）
- 60-69点: D（合格）
- 60点未満: F（不合格）
- 0未満または100を超える場合: エラーメッセージ

さらに、複数の学生の点数を処理し、クラスの平均点も計算してください。

```rust
fn main() {
    let scores = vec![95, 82, 70, 58, 45, 100, -5, 105, 88, 76];

    // 各学生の成績を判定
    for (i, &score) in scores.iter().enumerate() {
        // ここにコードを書いてください
    }

    // クラスの平均点を計算（無効な点数は除外）
    // ここにコードを書いてください
}

// 必要な関数を実装してください
```

---

## 提出方法

1. 各問題の解答を `phase1_answers.md` というファイルに記載してください
2. コーディング問題は実際に動作確認を行ってください
3. すべての解答が完了したら、Gitにコミットしてください：
   ```bash
   git add .
   git commit -m "Complete Phase 1 quiz"
   ```

---

## 評価基準

- 理解度確認問題（40点）：各問題の理解度と説明の正確さ
- コーディング問題（60点）：
  - コードが正しく動作するか（機能性）
  - Rustらしい書き方ができているか（慣用的な表現）
  - エラー処理が適切か
  - コードの可読性

合計70点以上で合格とします。

頑張ってください！
